/* eslint-disable */
import handshake from 'handshake-util'
import bitcoin from 'bitcoinjs-lib'

const generateAddress = (child, options) => {
  console.log('>>>> Call handshake generateAddress', child)
}



export default generateAddress